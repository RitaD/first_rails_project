<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Best comments</h2>

<%= render partial: 'comments/best_comment', collection: @article.best_comments %>

<h2>Comments</h2>

<%= render @article.plain_comments %>

-----------------

<% @article.comments.each do |comment| %>
  <%# render comment %>
<% end %>

-----------------

<% @article.comments.each do |comment| %>
  <%# render 'comments/comment', comment: comment %>
<% end %>
 
<h2>Add a comment:</h2>
<%= render 'comments/form' %>
 
<%= link_to 'Back', articles_path %> |
<%= link_to 'Edit', edit_article_path(@article) %>
